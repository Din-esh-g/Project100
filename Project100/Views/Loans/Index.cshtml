@model IEnumerable<Project100.Models.Loan>

@{
    ViewData["Title"] = "Index";
}

<h1 style=" text-align:center">Loan Accounts</h1>

<div tyle=" text-align:center">

    <img src="~/image/logo.png" alt="Revature Bank " height="150" width="200">
</div>
<br />
<a class="btn btn-info" role="button" asp-action="create ">Open New Account</a> |

<strong>@ViewData["ErrorMessage"]</strong>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InterestRate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Balance)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.createdAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.accountNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.type)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.InterestRate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Balance)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.createdAt)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.accountNumber)
                </td>
                <td>


                    <a asp-action="Details" class="btn btn-info" role="button" asp-route-id="@item.accountNumber">Details</a> |
                    <a asp-action="Delete" class="btn btn-info" role="button" asp-route-id="@item.accountNumber">Closed </a> |

                    <a class="btn btn-info" role="button" asp-controller="Transactions" asp-action="DisplayRecord" asp-route-id="@item.accountNumber">Statement</a>

                </td>
            </tr>
        }
    </tbody>
</table>
